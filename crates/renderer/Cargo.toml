[package]
name = "nexus-renderer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Terminal renderer for Nexus pages"

[lib]
name = "nexus_renderer"
path = "src/lib.rs"

[dependencies]
nexus-content = { path = "../content" }

[dev-dependencies]
serde_json = "1"
