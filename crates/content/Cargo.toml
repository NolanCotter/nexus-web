[package]
name = "nexus-content"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Structured page model (not HTML) and content IDs"

[lib]
name = "nexus_content"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
blake3 = "1"
hex = "0.4"
thiserror = "1"

[lints]
workspace = true
