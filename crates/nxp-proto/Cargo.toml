[package]
name = "nxp-proto"
version = "0.1.0"
edition = "2021"
description = "NXP/0.1 wire format primitives and parser skeleton (prototype)"

[dependencies]
tokio = { version = "1", features = ["io-util"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }